diff --git a/node_modules/@aws-sdk/lib-storage/dist-cjs/index.js b/node_modules/@aws-sdk/lib-storage/dist-cjs/index.js
index da417a0..d6a4373 100644
--- a/node_modules/@aws-sdk/lib-storage/dist-cjs/index.js
+++ b/node_modules/@aws-sdk/lib-storage/dist-cjs/index.js
@@ -169,6 +169,57 @@ var getChunk = /* @__PURE__ */ __name((data, partSize) => {
   );
 }, "getChunk");
 
+// src/signal.ts
+/**
+ * This function wires an external abort signal to an internal abort controller.
+ * The internal abort controller will be aborted when the external signal is
+ * aborted.
+ *
+ * Every callback created will be removed as soon as either the internal or
+ * external signal is aborted. This allows to avoid memory leaks, especially if
+ * the external signal has a (significantly) longer lifespan than the internal
+ * one.
+ *
+ * In order to ensure that any references are removed, make sure to always
+ * `abort()` the internal controller when you are done with it.
+ */
+function wireSignal(internalController, externalSignal){
+  if (!externalSignal || internalController.signal.aborted) {
+    return;
+  }
+  if (externalSignal.aborted) {
+    internalController.abort();
+    return;
+  }
+
+  if (isNativeSignal(externalSignal)) {
+    externalSignal.addEventListener("abort", () => internalController.abort(), {
+      once: true,
+      signal: internalController.signal,
+    });
+  } else {
+    // backwards compatibility
+    const origOnabort = externalSignal.onabort;
+    const restore = () => {
+      externalSignal.onabort = origOnabort;
+    };
+
+    externalSignal.onabort = function () {
+      internalController.abort();
+      restore();
+      origOnabort?.call(this);
+    };
+
+    // Let's clear any reference to the internal controller when it is aborted,
+    // avoiding potential memory leaks.
+    internalController.signal.addEventListener("abort", restore, { once: true });
+  }
+}
+
+function isNativeSignal(signal) {
+  return "addEventListener" in signal && typeof signal.addEventListener === "function";
+}
+
 // src/Upload.ts
 var MIN_PART_SIZE = 1024 * 1024 * 5;
 var _Upload = class _Upload extends import_events.EventEmitter {
@@ -183,7 +234,6 @@ var _Upload = class _Upload extends import_events.EventEmitter {
     this.partSize = MIN_PART_SIZE;
     this.leavePartsOnError = false;
     this.tags = [];
-    this.concurrentUploaders = [];
     this.abortMultipartUploadCommand = null;
     this.uploadedParts = [];
     this.uploadEnqueuedPartsCount = 0;
@@ -198,7 +248,9 @@ var _Upload = class _Upload extends import_events.EventEmitter {
     this.__validateInput();
     this.totalBytes = byteLength(this.params.Body);
     this.bytesUploadedSoFar = 0;
-    this.abortController = options.abortController ?? new import_abort_controller.AbortController();
+    this.abortController = new AbortController();
+    wireSignal(this.abortController, options.abortSignal);
+    wireSignal(this.abortController, options.abortController?.signal);
   }
   async abort() {
     this.abortController.abort();
@@ -210,7 +262,11 @@ var _Upload = class _Upload extends import_events.EventEmitter {
       );
     }
     this.sent = true;
-    return await Promise.race([this.__doMultipartUpload(), this.__abortTimeout(this.abortController.signal)]);
+    try {
+      return await this.__doMultipartUpload();
+    } finally {
+      this.abortController.abort();
+    }
   }
   on(event, listener) {
     this.uploadEvent = event;
@@ -237,7 +293,12 @@ var _Upload = class _Upload extends import_events.EventEmitter {
     if (eventEmitter !== null) {
       eventEmitter.on("xhr.upload.progress", uploadEventListener);
     }
-    const resolved = await Promise.all([this.client.send(new import_client_s3.PutObjectCommand(params)), (_a = clientConfig == null ? void 0 : clientConfig.endpoint) == null ? void 0 : _a.call(clientConfig)]);
+    const resolved = await Promise.all([
+      this.client.send(new import_client_s3.PutObjectCommand(params), {
+        abortSignal: this.abortController.signal,
+      }),
+      clientConfig?.endpoint?.(),
+    ]);
     const putResult = resolved[0];
     let endpoint = resolved[1];
     if (!endpoint) {
@@ -350,7 +411,10 @@ var _Upload = class _Upload extends import_events.EventEmitter {
           UploadId: this.uploadId,
           Body: dataPart.data,
           PartNumber: dataPart.partNumber
-        })
+        }),
+        {
+          abortSignal: this.abortController.signal,
+        }
       );
       if (eventEmitter !== null) {
         eventEmitter.off("xhr.upload.progress", uploadEventListener);
@@ -385,17 +449,16 @@ var _Upload = class _Upload extends import_events.EventEmitter {
   }
   async __doMultipartUpload() {
     const dataFeeder = getChunk(this.params.Body, this.partSize);
-    const concurrentUploaderFailures = [];
+    const concurrentUploads = [];
     for (let index = 0; index < this.queueSize; index++) {
-      const currentUpload = this.__doConcurrentUpload(dataFeeder).catch((err) => {
-        concurrentUploaderFailures.push(err);
-      });
-      this.concurrentUploaders.push(currentUpload);
+      const currentUpload = this.__doConcurrentUpload(dataFeeder);
+      concurrentUploads.push(currentUpload);
     }
-    await Promise.all(this.concurrentUploaders);
-    if (concurrentUploaderFailures.length >= 1) {
+    const results = await Promise.allSettled(concurrentUploads);
+    const firstFailure = results.find((result) => result.status === "rejected");
+    if (firstFailure) {
       await this.markUploadAsAborted();
-      throw concurrentUploaderFailures[0];
+      throw firstFailure.reason;
     }
     if (this.abortController.signal.aborted) {
       await this.markUploadAsAborted();
@@ -448,15 +511,6 @@ var _Upload = class _Upload extends import_events.EventEmitter {
       this.emit(this.uploadEvent, progress);
     }
   }
-  async __abortTimeout(abortSignal) {
-    return new Promise((resolve, reject) => {
-      abortSignal.onabort = () => {
-        const abortError = new Error("Upload aborted.");
-        abortError.name = "AbortError";
-        reject(abortError);
-      };
-    });
-  }
   __validateInput() {
     if (!this.params) {
       throw new Error(`InputError: Upload requires params to be passed to upload.`);
diff --git a/node_modules/@aws-sdk/lib-storage/dist-es/Upload.js b/node_modules/@aws-sdk/lib-storage/dist-es/Upload.js
index 38d122b..ff6e184 100644
--- a/node_modules/@aws-sdk/lib-storage/dist-es/Upload.js
+++ b/node_modules/@aws-sdk/lib-storage/dist-es/Upload.js
@@ -1,10 +1,10 @@
 import { AbortMultipartUploadCommand, CompleteMultipartUploadCommand, CreateMultipartUploadCommand, PutObjectCommand, PutObjectTaggingCommand, UploadPartCommand, } from "@aws-sdk/client-s3";
-import { AbortController } from "@smithy/abort-controller";
 import { getEndpointFromInstructions, toEndpointV1, } from "@smithy/middleware-endpoint";
 import { extendedEncodeURIComponent } from "@smithy/smithy-client";
 import { EventEmitter } from "events";
 import { byteLength } from "./bytelength";
 import { getChunk } from "./chunker";
+import { wireSignal } from "./signal";
 const MIN_PART_SIZE = 1024 * 1024 * 5;
 export class Upload extends EventEmitter {
     constructor(options) {
@@ -14,7 +14,6 @@ export class Upload extends EventEmitter {
         this.partSize = MIN_PART_SIZE;
         this.leavePartsOnError = false;
         this.tags = [];
-        this.concurrentUploaders = [];
         this.abortMultipartUploadCommand = null;
         this.uploadedParts = [];
         this.uploadEnqueuedPartsCount = 0;
@@ -30,6 +29,9 @@ export class Upload extends EventEmitter {
         this.totalBytes = byteLength(this.params.Body);
         this.bytesUploadedSoFar = 0;
         this.abortController = options.abortController ?? new AbortController();
+
+        wireSignal(this.abortController, options.abortSignal);
+        wireSignal(this.abortController, options.abortController?.signal);
     }
     async abort() {
         this.abortController.abort();
@@ -39,7 +41,11 @@ export class Upload extends EventEmitter {
             throw new Error("@aws-sdk/lib-storage: this instance of Upload has already executed .done(). Create a new instance.");
         }
         this.sent = true;
-        return await Promise.race([this.__doMultipartUpload(), this.__abortTimeout(this.abortController.signal)]);
+        try {
+            return await this.__doMultipartUpload();
+        } finally {
+            this.abortController.abort();
+        }
     }
     on(event, listener) {
         this.uploadEvent = event;
@@ -65,7 +71,12 @@ export class Upload extends EventEmitter {
         if (eventEmitter !== null) {
             eventEmitter.on("xhr.upload.progress", uploadEventListener);
         }
-        const resolved = await Promise.all([this.client.send(new PutObjectCommand(params)), clientConfig?.endpoint?.()]);
+        const resolved = await Promise.all([
+            this.client.send(new PutObjectCommand(params), {
+                abortSignal: this.abortController.signal,
+            }),
+            clientConfig?.endpoint?.(),
+        ]);
         const putResult = resolved[0];
         let endpoint = resolved[1];
         if (!endpoint) {
@@ -176,7 +187,7 @@ export class Upload extends EventEmitter {
                 UploadId: this.uploadId,
                 Body: dataPart.data,
                 PartNumber: dataPart.partNumber,
-            }));
+            }), { abortSignal: this.abortController.signal });
             if (eventEmitter !== null) {
                 eventEmitter.off("xhr.upload.progress", uploadEventListener);
             }
@@ -208,17 +219,16 @@ export class Upload extends EventEmitter {
     }
     async __doMultipartUpload() {
         const dataFeeder = getChunk(this.params.Body, this.partSize);
-        const concurrentUploaderFailures = [];
+        const concurrentUploads = [];
         for (let index = 0; index < this.queueSize; index++) {
-            const currentUpload = this.__doConcurrentUpload(dataFeeder).catch((err) => {
-                concurrentUploaderFailures.push(err);
-            });
-            this.concurrentUploaders.push(currentUpload);
+            const currentUpload = this.__doConcurrentUpload(dataFeeder);
+            concurrentUploads.push(currentUpload);
         }
-        await Promise.all(this.concurrentUploaders);
-        if (concurrentUploaderFailures.length >= 1) {
+        const results = await Promise.allSettled(concurrentUploads);
+        const firstFailure = results.find((result) => result.status === "rejected");
+        if (firstFailure) {
             await this.markUploadAsAborted();
-            throw concurrentUploaderFailures[0];
+            throw firstFailure.reason;
         }
         if (this.abortController.signal.aborted) {
             await this.markUploadAsAborted();
@@ -265,15 +275,6 @@ export class Upload extends EventEmitter {
             this.emit(this.uploadEvent, progress);
         }
     }
-    async __abortTimeout(abortSignal) {
-        return new Promise((resolve, reject) => {
-            abortSignal.onabort = () => {
-                const abortError = new Error("Upload aborted.");
-                abortError.name = "AbortError";
-                reject(abortError);
-            };
-        });
-    }
     __validateInput() {
         if (!this.params) {
             throw new Error(`InputError: Upload requires params to be passed to upload.`);
diff --git a/node_modules/@aws-sdk/lib-storage/dist-es/signal.js b/node_modules/@aws-sdk/lib-storage/dist-es/signal.js
new file mode 100644
index 0000000..70e0ad1
--- /dev/null
+++ b/node_modules/@aws-sdk/lib-storage/dist-es/signal.js
@@ -0,0 +1,49 @@
+/**
+ * This function wires an external abort signal to an internal abort controller.
+ * The internal abort controller will be aborted when the external signal is
+ * aborted.
+ *
+ * Every callback created will be removed as soon as either the internal or
+ * external signal is aborted. This allows to avoid memory leaks, especially if
+ * the external signal has a (significantly) longer lifespan than the internal
+ * one.
+ *
+ * In order to ensure that any references are removed, make sure to always
+ * `abort()` the internal controller when you are done with it.
+ */
+export function wireSignal(internalController, externalSignal){
+  if (!externalSignal || internalController.signal.aborted) {
+    return;
+  }
+  if (externalSignal.aborted) {
+    internalController.abort();
+    return;
+  }
+
+  if (isNativeSignal(externalSignal)) {
+    externalSignal.addEventListener("abort", () => internalController.abort(), {
+      once: true,
+      signal: internalController.signal,
+    });
+  } else {
+    // backwards compatibility
+    const origOnabort = externalSignal.onabort;
+    const restore = () => {
+      externalSignal.onabort = origOnabort;
+    };
+
+    externalSignal.onabort = function () {
+      internalController.abort();
+      restore();
+      origOnabort?.call(this);
+    };
+
+    // Let's clear any reference to the internal controller when it is aborted,
+    // avoiding potential memory leaks.
+    internalController.signal.addEventListener("abort", restore, { once: true });
+  }
+}
+
+export function isNativeSignal(signal) {
+  return "addEventListener" in signal && typeof signal.addEventListener === "function";
+}
\ No newline at end of file
